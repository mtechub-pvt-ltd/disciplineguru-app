import {TestIds} from 'react-native-google-mobile-ads';

export const bannerAdID = TestIds.BANNER; // banner ad id

export const fullScreenAdId = TestIds.INTERSTITIAL; // full screen ad id

export const rewardedAdId = TestIds.REWARDED; // video ad id
